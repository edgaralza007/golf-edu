# The Anthropic API key is NOT stored here.
# Add ANTHROPIC_API_KEY (no VITE_ prefix) to your Netlify site's
# Environment Variables in the Netlify dashboard. It will be injected
# into the serverless function at runtime and never reach the browser.

# Optional: override the model used by Shot Coach
# Options: claude-opus-4-6 | claude-sonnet-4-6 | claude-haiku-4-5-20251001
VITE_COACH_MODEL=claude-sonnet-4-6
